/*
import http from '/utils/http'

const USER_API = process.env.API_USER;

//查询代理网点信息
export function login(data) {
  return http({
    url: USER_API + 'user/login',
    method: 'post',
    data: data,
  });
}
*/
